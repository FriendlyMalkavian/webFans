import BaseModule from "../BaseModule";
import { BaseOptions } from "../BaseModule";

class PublicationModule extends BaseModule {
    constructor(options: BaseOptions) {
        super(options);
    }
}

export default PublicationModule;